server:
  port: ${SERVER_PORT}

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI}

springdoc:
  swagger-ui:
    enabled: false

management:
  endpoint:
    health:
      show-details: when_authorized

